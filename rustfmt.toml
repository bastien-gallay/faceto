# Stable-only options so `cargo fmt` behaves identically on every contributor's
# pinned toolchain (no nightly-gated settings).
edition = "2021"
max_width = 100
newline_style = "Unix"
use_small_heuristics = "Default"
reorder_imports = true
